# Application
PORT=3000
JWT_SECRET=change-me

# MySQL (untuk app)
DB_HOST=localhost
DB_USER=appuser
DB_PASSWORD=apppass
DB_NAME=stok_kuota

# Docker Compose (untuk service db)
MYSQL_ROOT_PASSWORD=rootpass
# DB_USER / DB_PASSWORD / DB_NAME di atas juga dipakai oleh compose